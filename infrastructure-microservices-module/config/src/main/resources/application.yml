
server:
  port: 8888

---
spring:
  profiles:
    active: prod #dev
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared

  security:
    user:
      password: ${CONFIG_SERVICE_PASSWORD}

---
spring:
  profiles:
    active: dev #prod!
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IlyaChakun/panteon-trans/
          search-paths:
            - "services-config-files/{profile}"
          username: IlyaChakun
          password: 1999IlyaChekun
      label: master
  security:
    user:
      password: ${CONFIG_SERVICE_PASSWORD}
