server:
  port: 8080

spring:
  application:
    name: api-gateway-service
  cloud:
    config:
      enabled: true
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user